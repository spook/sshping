Building:
    Install libssh-dev package


sshing -u username -p pass -i key    host:port(22)


Options:
  -t Time to run then exit
  -xxx?  Bursts length/duration (or mode to simulate a typing person)

Measures:
   Initial connect time (handshake time)
   Character echo latency  (min, max, avg, median + variance)
   Stats like number of chars


